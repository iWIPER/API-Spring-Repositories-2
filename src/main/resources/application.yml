spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:8080/newbd
    username: developer
    password: password
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: update
  jackson:
    serialization:
      fail-on-empty-beans: false
server:
  port: 5050
